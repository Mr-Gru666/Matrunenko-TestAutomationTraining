name: Run Tests and Generate Allure Report

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Cache Gradle packages
        uses: actions/cache@v3
        with:
          path: ~/.gradle/caches
          key: gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            gradle-

      - name: Grant execute permission for Gradle
        run: chmod +x gradlew

      - name: Run tests with Gradle
        run: ./gradlew clean test
        continue-on-error: true  # Позволяет продолжить выполнение, даже если тесты упадут

      - name: Generate Allure Report
        run: ./gradlew allureReport
        if: always()  # Всегда выполняется, даже если тесты упали

      - name: Upload Allure Results
        uses: actions/upload-artifact@v3
        with:
          name: allure-report
          path: build/reports/allure-report

  deploy:
    needs: test
    if: always()  # Деплой отчета всегда выполняется, даже если test упал
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download Allure Report
        uses: actions/download-artifact@v3
        with:
          name: allure-report
          path: allure-results

      - name: Deploy Allure Report to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./allure-results
